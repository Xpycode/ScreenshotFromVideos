# PROJECT_STATE — ScreenshotFromVideos

## What this is
macOS app that extracts still frames from a video as PNGs — every Nth frame or user-selected timestamps — with optional timestamp overlay and chronological filename numbering. Built primarily to feed Claude Code visual context from screen recordings.

**Design target: short clips, 30 s to 2 min.** Longer videos (10+ min) work but the thumbnail-strip rendering is visibly slower since the asset gets walked through more keyframes per scroll. Optimizing for multi-hour clips is explicitly out of scope.

**User-facing name:** "Stills From Video" (since 2026-05-27 — see Key Decisions). **Internal identifiers** (bundle ID `com.lucesumbrarum.ScreenshotFromVideos`, repo folder, Xcode target, scheme) intentionally kept as `ScreenshotFromVideos`.

## Current Focus
**WebP export support — Waves A–D ALL DONE 2026-05-29 on `polish/webp-support`. Feature functionally complete + smoke-verified. Remaining: housekeeping (promote decisions, merge/push).** Plan: `POLISH_PLAN_webp_support.md` at repo root.

**Wave D smoke PASSED** on user's 4K HEVC Rec.709 clip (`…/GPSvideo/04_Exports/SampleVideos/X-H2S…XH2S6508.MOV`), run via a throwaway SwiftPM exe vendoring `WebPEncoder.swift` verbatim (app can't be driven headlessly — SDWebImageWebPCoder static-linked into the Mach-O). Lossy works (q0.5→96 KB, q1.0→663 KB, error drops as quality rises); all valid RIFF WebP; effort knob works (e1.0 < e0.5); no color cast in side-by-side. **One honest correction:** lossless is **near-lossless, NOT bit-exact** — even via libwebp's reference `dwebp` it differs from the source PNG by maxAbs=5/255 (mean 0.34); the shift is in the `CGImage→NSImage→SDWebImage` encode hand-off (the two effort levels are bit-exact to each other, so lossless is deterministic). Sub-perceptual (~2%), ~57% smaller than PNG. Per user decision: accept encoder as-is, fixed the tooltip (`cf7a98b`) from "bit-exact pixels, 25–35% smaller" to "visually identical (within ~2%), ~55% smaller".

**Waves B + C summary** (3 commits after Wave A):
- `7fe7783 feat(wave-b): WebP encoder + model plumbing` — fleshed out `WebPEncoder.encode` (sRGB CGContext conversion → NSImage wrap → options dict → `encodedData` → `Data.write`); extended `ExportFormat` with `.webp` + `hasLosslessOption`; added `lossless` to `ExtractionRequest`; `exportLossless` to `Preferences` + VM; `ImageExportService.writeImage` gained `lossless:` + WebP branch; pipeline forwards the flag; fixed the pre-existing hardcoded `"wrote N PNG"` → `request.format.rawValue`.
- `a9e53fe feat(wave-c): WebP UI` — Lossless toggle (gated on `hasLosslessOption`, placed above slider) + Quality↔Effort label flip in `RightPaneView.formatSection`. WebP button appears automatically from `allCases`.
- `207b2a6 fix(wave-c): separate WebP-lossless effort from shared quality` — user smoke caught that the single shared `exportQuality` made JPG's 33% quality leak into WebP-lossless "effort". Split: JPG/HEIC/WebP-lossy share `exportQuality` (original decision); WebP-lossless effort gets its own `exportEffort` (default 0.75). Slider binds to the active value via ternary; `buildRequest` feeds effort only in WebP+lossless mode. User confirmed fixed.

**Three load-bearing findings from execution (promote to decisions.md):**
1. **SDWebImageWebPCoder 0.15.0 renamed the option constants** the plan spec'd: use `SDImageCoderOption.encodeWebPMethod` / `.encodeWebPLossless` (member-shorthand), NOT `SDImageCoderEncodeWebPMethod` / `SDImageCoderEncodeWebPLossless`. Build fails otherwise.
2. **WebP-lossless effort is stored separately from quality** (`exportEffort` default 0.75). The slider is one control with two semantic roles; the backing value must differ so the two axes don't clobber each other.
3. **Our WebP "lossless" is near-lossless (≤5/255), not bit-exact** to the source. Confirmed via libwebp's reference `dwebp` (so it's not a decode artifact) — the ≤5 shift enters in the `CGImage→NSImage→SDWebImage` encode hand-off, before libwebp. Imperceptible (~2%) and still ~57% smaller than PNG; UI tooltip says "visually identical", not "bit-exact". True bit-exact would require feeding sRGB RGBA straight to libwebp (bypassing NSImage) — declined as not worth it.

Build green under `SWIFT_STRICT_CONCURRENCY=complete` at every step; SourceKit's "No such module 'SDWebImage'" diagnostics throughout are SPM-invisible-to-SourceKit noise (xcodebuild clean), same as Wave A.

**Wave A summary** (2 commits, 32 LOC of code + planning docs):
- `ce5b0aa docs: WebP export plan + session log` carries yesterday's planning docs onto the new branch.
- `5113627 feat(wave-a): wire SDWebImageWebPCoder via SPM + stub encoder` — added top-level `packages:` block + target `dependencies:` to `01_Project/project.yml`; created `Services/WebPEncoder.swift` (24 LOC stub with `@preconcurrency import` on both `SDWebImage` + `SDWebImageWebPCoder`; `encode(_:to:quality:lossless:) throws` body just throws `.failedToWriteImage` so the type exists for Wave B's wiring).
- `xcodebuild -resolvePackageDependencies` pinned: **SDWebImageWebPCoder 0.15.0 + SDWebImage 5.21.7 + libwebp 1.5.0**. Clean build green under `SWIFT_STRICT_CONCURRENCY=complete`; zero SFV-code warnings. `@preconcurrency import` pair is correctly suppressing the non-Sendable-headers noise per plan decision 6.
- Branch local-only, no remote.

**Encoder pick (recap):** SDWebImageWebPCoder 0.15.0 (Nov 2025) via SPM. **Option A** (full SDWebImage core dep) over Option B (bespoke libwebp-Xcode wrapper). Trade-off: ~2 MB binary growth for upstream maintenance + ICC-embed feature. Future-shrink path documented.

**UI surface (Wave C):** Affinity-minimal — Quality slider (reused as Effort in lossless mode via libwebp's quality-as-effort semantic; label **flips to "Effort"** when WebP + Lossless on, user-confirmed) + new Lossless toggle. Skipped: image_hint, method slider, alpha knobs.

**Three load-bearing technical decisions baked into the plan (Wave B lands them):**
1. **Hardcode `SDImageCoderEncodeWebPMethod: NSNumber(value: 6)`** on every encode (closes SDWebImageWebPCoder issue #116 quality gap vs `cwebp` CLI; default method=4 visibly worse).
2. **Convert CGImage to sRGB via transient `CGContext`** before encoder handoff (`AVAssetImageGenerator` returns Rec.709/BT.2020-tagged CGImages; libwebp writes pixels as-is; viewers without color management see shift). 0.15.0 then embeds the resulting sRGB ICC profile.
3. **`@preconcurrency import`** on both SDWebImage + SDWebImageWebPCoder modules — ✅ landed in Wave A; confirmed warning-free under `SWIFT_STRICT_CONCURRENCY=complete`.

**Plan shape:** ~95 LOC across 1 new file (`Services/WebPEncoder.swift`) + 7 modified (`project.yml`, `ExportFormat`, `ExtractionRequest`, `Preferences`, `ExtractionViewModel`, `ImageExportService`, `ExtractionPipeline`, `RightPaneView`). Five waves: A wiring + stub for clean build ✅, B encoder + model plumbing, C UI, D 7-step smoke including bit-exact lossless-vs-PNG byte compare, E commit (subsumed by the per-wave commit pattern).

**Pre-existing bug surfaced during recon (will fix in Wave B):** `ExtractionViewModel.startExtraction` line 239 hardcodes `"wrote N PNG"` regardless of format — silently wrong for JPG/HEIC since the Item-2 multi-format ship 2026-05-28. One-line fix: `"PNG"` → `request.format.rawValue`.

---

**Post-Phase-5 polish items 1, 2, 3 SHIPPED 2026-05-28** on branch `polish/post-phase-5` (3 atomic commits, ~180 LOC net, build green, user-smoked live):

- **Item 3** (`75bb7c7`, +4/-3, 1 file) — Unload button labeled, tooltip clarified
- **Item 1** (`f2d2eaf`, +51/-18, 3 files) — Frame-count preview in export footer; `buildRequest` collapsed via new `currentMode` computed
- **Item 2** (`8b516e9`, +129/-7, 7 files inc. new `Models/ExportFormat.swift`) — Multi-format export PNG/JPG/HEIC + quality slider. WebP omitted at the time (ImageIO can't write it on macOS 26.5) — **now being reopened via SDWebImageWebPCoder**, see top of Current Focus.

Branch is local-only; no remote configured. `POLISH_PLAN_post_phase5.md` is fully executed and ready for archival.

**Findings worth promoting** (not yet in `docs/decisions.md`):
1. **ImageIO-can't-write-WebP runtime-probe finding** (load-bearing — preserve regardless of plan reversal; it's the reason a third-party encoder is needed at all).
2. **xcodegen regen IS required for new files** in this repo — ✅ promoted to `docs/decisions.md` 2026-05-28; reconfirmed during Wave A (editing `project.yml`'s `packages:` block required `xcodegen generate` before the next build to wire SPM into the pbxproj).

**One blocker surfaced and fixed during Phase 5 Wave 6.1(c)** (kept for historical reference): fast pinch on a 5-min clip saturated VTDecoderXPCService at 430% CPU / 465 threads. Root cause: `AVAssetImageGenerator.images(for:)` doesn't honor Swift `Task` cancellation promptly. Two fixes shipped together: (1) `withTaskCancellationHandler { generator.cancelAllCGImageGeneration() }` in `ThumbnailRenderer`; (2) 256-cap on requested times per batch in `StripModel.scheduleRender`, prioritized by distance to visible center. Pattern saved as memory ([[feedback_avf_image_generator_cancellation]]).

## Last Session
2026-05-29 (latest, same day) — **Wave D encode smoke PASSED — WebP export feature complete.** User supplied a 4K HEVC Rec.709 clip (`…/GPSvideo/04_Exports/SampleVideos/X-H2S…XH2S6508.MOV`). App can't be driven headlessly (no CLI; SDWebImageWebPCoder static-linked into the Mach-O — `SDWebImageWebPCoder.o`/`libwebp.o` in Build/Products, no `.framework`), so ran the encode matrix via a throwaway SwiftPM exe at `/tmp/webpsmoke` vendoring `WebPEncoder.swift` verbatim + the same SDWebImageWebPCoder 0.15.0 (pattern saved as memory [[feedback_headless_spm_encoder_test]]). Lossy: q0.5→96 KB (mean err 0.99), q1.0→663 KB (0.47, better ✓); all four valid `RIFF … Web/P image`; lossless e1.0 (3.67 MB) < e0.5 (3.68 MB) so effort knob works; `_compare.png` (jackdaw on grass, PNG vs lossy-q50) shows no color cast. **Honest correction: lossless is near-lossless, NOT bit-exact** — even via libwebp's reference `dwebp` it differs from the source PNG by maxAbs=5/255 (mean 0.34); the two effort levels are bit-exact to each other (dwebp e50 vs e100 maxAbs=0), so the ≤5 shift enters in the `CGImage→NSImage→SDWebImage` encode hand-off, before libwebp. Plan criterion #4 (bit-exact) does not hold; the delta is sub-perceptual (~2%) and lossless is still ~57% smaller than PNG. Per user decision (AskUserQuestion): accept the encoder as-is, fix the tooltip (`cf7a98b`: "bit-exact pixels, 25–35% smaller" → "visually identical (within ~2%), ~55% smaller"). A true-bit-exact path (sRGB RGBA straight to libwebp, bypassing NSImage) was offered and declined. **6 code commits (A/B/C + effort-split + tooltip) + 4 doc commits on `polish/webp-support`; local-only, no remote.** Remaining = housekeeping: promote the now-3 execution findings + 5 plan decisions to `docs/decisions.md`; merge/push decision.

2026-05-29 (earlier, same day) — **Execution session: WebP Waves B + C + effort-split fix SHIPPED.** User drove wave-by-wave ("execute Wave B" → "commit Wave B then do Wave C" → visual check → effort-split fix → "commit it" → "update docs"). Three commits on `polish/webp-support`, build green under `SWIFT_STRICT_CONCURRENCY=complete` throughout. **Wave B** (`7fe7783`, 7 files ~99 ins): real `WebPEncoder.encode` (transient sRGB `CGContext` premultipliedLast|order32Big → `makeImage()` → `NSImage(cgImage:size:.zero)` → options dict → `SDImageWebPCoder.shared.encodedData(with:format:.webP,options:)` → `Data.write`); `ExportFormat.webp` + `hasLosslessOption`; `ExtractionRequest.lossless`; `Preferences.exportLossless` + VM property; `ImageExportService.writeImage(...,lossless:)` with early WebP branch; pipeline forwards `request.lossless`; bonus fix of the hardcoded `"wrote N PNG"` message → `request.format.rawValue`. **Build snag worth remembering:** SDWebImageWebPCoder 0.15.0 **renamed** the option constants — `SDImageCoderEncodeWebPMethod` → `SDImageCoderOption.encodeWebPMethod`, `...Lossless` → `.encodeWebPLossless`; used member-shorthand since the dict is `[SDImageCoderOption: Any]`; the plan referenced the pre-rename names. **Wave C** (`a9e53fe`): conditional Lossless toggle (above the slider) + Quality↔Effort label flip in `RightPaneView.formatSection`; WebP button auto-appears from `allCases`. Plan described a `qualityRow` ViewBuilder that doesn't exist — the real slider is inline; adapted to actual code. **Effort-split fix** (`207b2a6`, 3 files): user smoke caught the single shared `exportQuality` leaking JPG's 33% into WebP-lossless "effort". Per AskUserQuestion the user chose to split — JPG/HEIC/WebP-lossy keep sharing `exportQuality`, WebP-lossless effort gets its own `exportEffort` (default 0.75); slider binds active value via ternary, `buildRequest` feeds effort only in WebP+lossless. App launched twice for visual verification; user confirmed all controls behave + the effort split works. **No frame has actually been WebP-encoded yet — that's Wave D, blocked on a Rec.709 SDR test clip from the user.**

2026-05-29 (later same day) — **Execution session: Wave A SHIPPED on fresh `polish/webp-support` branch.** User returned with `/execute next wave`. With nothing yet shipped from the plan written earlier the same day, "next wave" = Wave A — the safe wiring wave (~10 min, clean build, no functional change). Branched off `main` (deliberately not stacked on `polish/post-phase-5`, per 2026-05-29 user decision). Yesterday's uncommitted planning docs (`POLISH_PLAN_webp_support.md`, this session log, `_index.md` + `PROJECT_STATE.md` updates) committed first as `ce5b0aa docs: WebP export plan + session log`. Wave A code change committed as `5113627 feat(wave-a): wire SDWebImageWebPCoder via SPM + stub encoder`, +32 LOC across 2 files: (1) `01_Project/project.yml` — added top-level `packages: { SDWebImageWebPCoder: { url: …, from: 0.15.0 } }` block + `dependencies: [{ package: SDWebImageWebPCoder, product: SDWebImageWebPCoder }]` under `targets.ScreenshotFromVideos`. (2) `01_Project/ScreenshotFromVideos/Services/WebPEncoder.swift` — NEW, 24 LOC stub. `@preconcurrency import SDWebImage` + `@preconcurrency import SDWebImageWebPCoder` (the two-module @preconcurrency is the load-bearing piece — without it, both modules' non-Sendable headers light up under `SWIFT_STRICT_CONCURRENCY=complete`). Stub `encode(_:to:quality:lossless:) throws` body just throws `ImageExportError.failedToWriteImage` so the type exists for Wave B's wiring without breaking the build mid-wave. Header comment notes Wave A is intentionally stub-only and that the full encode (sRGB CGContext → NSImage wrap → options dict with hardcoded `method=6` → `SDImageWebPCoder.shared.encodedData` → `Data.write`) lands in Wave B. Build verification: `xcodegen generate` regenerated the pbxproj cleanly (pbxproj is gitignored — `project.yml` is the source of truth, confirmed via `.gitignore` listing `01_Project/*.xcodeproj`). `xcodebuild -resolvePackageDependencies` fetched + pinned **SDWebImageWebPCoder 0.15.0** + **SDWebImage 5.21.7** + **libwebp 1.5.0** — all source-built, no xcframework, no dlopen, so notarization + hardened-runtime stay clean per plan's "Build & sign sanity" section. `xcodebuild clean build` succeeded under `SWIFT_STRICT_CONCURRENCY=complete`; grep over the full build log for warnings/errors on SFV's own code (excluding SPM-dep noise) returned **zero** — the `@preconcurrency import` pair is correctly suppressing the unaudited-headers noise. **One SourceKit diagnostic mid-edit** ("No such module 'SDWebImage'") before `xcodegen generate` ran — expected behavior (SourceKit can't see SPM products until the pbxproj is regenerated), worth noting if future-me sees a similar diagnostic during a new SPM wiring. Wave A success criteria all met: SPM resolves cleanly, stub compiles, zero SFV warnings. **Next:** Wave B — flesh out `WebPEncoder.encode` (sRGB CGContext conversion → `NSImage(cgImage:size:)` wrap → options dict with hardcoded `SDImageCoderEncodeWebPMethod: 6` + optional `SDImageCoderEncodeWebPLossless: true` → `SDImageWebPCoder.shared.encodedData(with:format:options:)` → `Data.write(to:)`); extend `Models/ExportFormat.swift` with `.webp = "WebP"` + `utType: .webP` + `fileExtension: "webp"` + `supportsCompression: true` + new computed `hasLosslessOption: Bool { self == .webp }`; add `var lossless: Bool = false` to `ExtractionRequest`; add `exportLossless` Key + reader/writer to `Preferences`; add `exportLossless` property to `ExtractionViewModel` with `didSet` write-through, populated from `Preferences` in `init()`, passed through `buildRequest()`; update `ImageExportService.writeImage` signature (add `lossless:` param + early-return `if format == .webp` branch routing to `WebPEncoder.encode`); update `ExtractionPipeline` call site to forward `lossless: request.lossless`. Bundle in the pre-existing bug fix: `ExtractionViewModel.startExtraction:239` `"PNG"` → `request.format.rawValue` (silently wrong for JPG/HEIC since Item-2 ship). Wave B is the largest wave by LOC (~60 of the plan's ~95).

2026-05-29 (earlier) — **Planning session: WebP export support.** No code shipped this session — user explicitly held for plan review after seeing the deliverable. /ultrathink + 2 parallel general-purpose research agents (SDWebImageWebPCoder recon → v0.15.0 / SPM YAML / Swift 6 `@preconcurrency` requirement / load-bearing issue #116 method=4 quality gap / color-space gotcha; libwebp UX recon → WebPConfig knob enumeration / Squoosh + GIMP + Photoshop + Affinity survey / Affinity-minimal Quality+Lossless recommendation) + direct SFV codebase recon mapping exact insertion points across 8 files. Conversation arc: user asked "what about webp, why doesn't it work, why is CropBatch hollow?" → I explained ImageIO decode-only + CropBatch silently writing nothing → user revealed paid Apple Developer Program account → I laid out Option A (SDWebImageWebPCoder, ~2 MB binary growth) vs Option B (bespoke libwebp-Xcode wrapper, ~50 LOC owned C-interop) with build/sign/notarization sanity for both → user picked Option A + "whatever the encoder allows" → /ultrathink planning. **Deliverable: `POLISH_PLAN_webp_support.md`** at repo root, 5 waves (A wiring with SPM resolution + stub encoder, B encoder + model plumbing + the pre-existing "wrote N PNG" message bugfix surfaced during recon, C UI with conditional Lossless toggle + Quality↔Effort label flip, D 7-step smoke including bit-exact lossless-vs-PNG byte compare + Rec.709 source for color-conversion verification, E commit on fresh `polish/webp-support` branch). User decisions captured live via AskUserQuestion: **(1)** Quality slider label flips to "Effort" when WebP + Lossless active (vs always-Quality-plus-tooltip — picked the more honest signal); **(2)** ship on a **fresh `polish/webp-support` branch**, not stacked on `polish/post-phase-5` (cleanly separable for review/revert); **(3)** hold for plan review — no execution this session. **Color-space gotcha worth its own callout**: `AVAssetImageGenerator` returns CGImages tagged with the video color space (Rec.709 for SDR, BT.2020 for HDR); libwebp writes pixels as-is; without sRGB conversion, color-unmanaged viewers see a shift. Plan includes a transient `CGContext` conversion before the NSImage handoff so SDWebImageWebPCoder 0.15.0 can embed the sRGB ICC profile of the converted image. **SDWebImageWebPCoder issue #116** is load-bearing — default `method=4` produces visibly lower output than the `cwebp` CLI at the same `q`; mitigation hardcoded into the plan is `SDImageCoderEncodeWebPMethod: NSNumber(value: 6)` on every encode (CPU cost 2–3× acceptable for batch). **`@preconcurrency import`** required on both SDWebImage core + SDWebImageWebPCoder since headers aren't Sendable-audited under our `SWIFT_STRICT_CONCURRENCY=complete`. **Pre-existing bug discovered**: `ExtractionViewModel.startExtraction` line 239 hardcodes `"wrote N PNG"` regardless of format — silently wrong for JPG/HEIC since the Item-2 multi-format ship 2026-05-28. One-line fix (`"PNG"` → `request.format.rawValue`) bundled into Wave B since the request shape is being touched anyway. **Estimated total LOC**: ~95 net across 1 new (`Services/WebPEncoder.swift` ~55 LOC self-contained — sRGB CGContext conversion → `NSImage(cgImage:size:)` wrap → options dict → `SDImageWebPCoder.shared.encodedData(...)` → `Data.write(to:)`) + 7 modified files. Parallels POLISH_PLAN_post_phase5 Item-2 (~120 LOC). Next time the user surfaces: either "ship it / execute" (needs a short Rec.709 SDR test clip for Wave D), "Wave A only" (~10 min, no functional change), or "edits" (revise the plan first).

2026-05-28 (latest) — **Execution session: polish items 3 → 1 → 2 ALL SHIPPED on branch `polish/post-phase-5`.** `/execute next plan` driven by the polish plan written earlier the same day. Three atomic commits in recommended ship order; each built green and user-smoked live before moving on. Item 3 (10 minutes, labeled Unload button + clarified tooltip) shipped first as planned. Item 1 (frame-count preview ~30 LOC) added `TimeListGenerator.count(for:duration:fps:)` plus `currentMode: ExtractionMode?` and `previewFrameCount: Int` on the VM (collapsing `buildRequest()` from ~20 lines to 9 in the process); new `Text("\(n) frame\(s) will be exported")` row at the top of `exportFooter`, gated on `metadata != nil && !isRunning`. User screenshots confirmed correctness across all three modes (Manual / Interval-Frames / Interval-Seconds). Item 2 (multi-format ~120 LOC across 7 files including new `Models/ExportFormat.swift`) was the largest. Hit one build snag worth recording: **xcodegen REGEN was required for the new `ExportFormat.swift` file** — first `xcodebuild` failed with "cannot find type 'ExportFormat'" because the sources glob is resolved at `xcodegen generate` time, not at build time. `POLISH_PLAN_post_phase5.md`'s "auto-discovers — no `xcodegen generate` needed unless build settings change" claim was wrong. Ran `xcodegen generate` from `01_Project/`, second build green. **Add to `docs/decisions.md`** so future polish sessions don't hit it. Item 2 implementation: enum with `.png/.jpeg/.heic` cases + `utType` + `fileExtension` (hardcoded `.jpeg → "jpg"` since `UTType.jpeg.preferredFilenameExtension` returns `"jpeg"`) + `supportsCompression`; `ImageExportService.writePNG → writeImage(_:to:format:quality:)` with conditional `kCGImageDestinationLossyCompressionQuality` only for compression-supporting formats; `ExtractionRequest.format/quality` fields; `exportFormat/exportQuality` VM properties with `didSet { Preferences.set...(...) }` + populated from `Preferences` in `init()`; pipeline uses `request.format.fileExtension` for URL builder + `writeImage(..., format:, quality:)` for encode; `formatSection` in RightPaneView between Output and Options — 3 bordered buttons with active-tint + conditional Quality row (label + Slider 0.1...1.0 step 0.05 + percentage readout) shown only for `supportsCompression`. User-smoked JPG @ 0/50/100% and HEIC @ 0/50/100% — 6 smoke-test export files left in repo root (`ExportHEIC0/50.heic`, `exportHEIC100.heic`, `ExportJPEG0/50/100.jpg`), flagged to user for cleanup, not committed. **Branch `polish/post-phase-5` is local-only; main has no remote configured. Polish plan fully executed, ready for archival.**

2026-05-28 (planning, earlier same day) — **Planning session: polish items 1–3.** User asked for a planned-out implementation of frame-count preview, multi-format export, and unload-button clarity, using agents/MCPs/web with ultrathink. Dispatched 3 parallel research streams (SFV codebase recon mapping `RightPaneView.exportFooter` / `ExtractionViewModel.buildRequest` / `ImageExportService.writePNG` / `LeftPaneView` footer / `TimeListGenerator.times`; CropBatch lift-target scan finding `ExportFormat` enum at `Models/ExportSettings.swift:5–40` + per-format `save()` at `ImageCropService.swift:738–764` + `QualitySlider` at `ExportSettingsView.swift:327–350`; Apple/web research via apple-doc-mcp + sosumi + WebSearch on `CGImageDestination` multi-format APIs). **Showstopper finding: ImageIO has no WebP write support on macOS through 26.5.** `UTType.webP` is a known UTI but `org.webmproject.webp` is absent from `CGImageDestinationCopyTypeIdentifiers()`. User pushed back ("how does cropbatch get webp export?") — fair challenge. Spawned verification round: runtime probe via `swift /tmp/probe.swift` on this Mac confirmed 22 writable types, WebP not among them (HEIC/JPEG/PNG/TIFF all present). CropBatch dep audit found `Package.resolved` lists only Sparkle 2.8.1, no libwebp/SDWebImage/WebPKit/vendored xcframework, no pre-export interception — error from `CGImageDestinationCreateWithURL` returning nil is logged at `CropBatchApp.swift:368` and execution silently continues. CropBatch's README claim of WebP support is hollow; no `.webp` file is ever written. **Deliverable: `POLISH_PLAN_post_phase5.md`** at repo root (mirrors `PHASE5_PLAN_thumbnail_strip.md` convention). Item 1 (frame-count preview, ~30 LOC, 3 files): new `TimeListGenerator.count(for:duration:fps:)` + `currentMode: ExtractionMode?` + `previewFrameCount: Int` computeds on VM (also de-dupes `buildRequest`'s mode resolution) + `Text("\(n) frame\(...) will be exported")` row at top of `exportFooter`. Item 2 (multi-format PNG/JPG/HEIC + quality slider, ~120 LOC, 7 files): new `Models/ExportFormat.swift` lifted from CropBatch (TIFF/WebP dropped, `fileExtension` overridden so `.jpeg` → `"jpg"` since `UTType.jpeg.preferredFilenameExtension` returns `"jpeg"`); `writePNG` → `writeImage(_:to:format:quality:)` with conditional `kCGImageDestinationLossyCompressionQuality` only for `supportsCompression`; `exportFormat` + `exportQuality` on VM persisted via existing `Preferences.swift` static-helper pattern; `formatSection` in RightPaneView with 3 bordered buttons + `Slider(value: $vm.exportQuality, in: 0.1...1.0, step: 0.05)` hidden when PNG selected; single shared quality across JPG and HEIC (revisit per-format only if HEIC files come out too big). Item 3 (unload button clarity, ~10 LOC, 1 file): `Image(systemName: "xmark.circle.fill")` → `Label("Unload", systemImage: "xmark.circle.fill")` with `.labelStyle(.titleAndIcon)`; tooltip clarified to "Remove this video from the app. The source file is not deleted." No menu item (⌘W belongs to window close), no confirmation dialog (operation non-destructive — `manualTimes` is transient by design). Decisions logged for promotion to `docs/decisions.md`: drop WebP with runtime-probe rationale; `.jpg` not `.jpeg`; single shared quality; PNG default; hide-not-disable quality slider when PNG; "Unload" beats "Close Video"/"Remove".

2026-05-28 (earlier) — **Phase 5 Wave 4.2 ⌘-click capture + bare-`M` shortcut + sorted-insert for manual list SHIPPED, and Wave 5 memory smoke PASSED, all user-confirmed.** Wave 5 procedure: started a 1 Hz RSS sampler via `ps -p $PID -o rss=` for 60 s while the user dropped their longest video, hit ⌘= to max zoom, and scrubbed the strip's vertical scrollbar. Baseline (no video) was 128 MB; first sample after load was 383 MB; peak during scrub was **466 MB at t+38s**; final 418 MB; min during the window 379 MB. Ceiling for this hardware (64 GB physical → 500 MB cache tier from `ThumbnailCache.tier()` + 200 MB overhead = 700 MB) gave ~33% headroom. The saw-tooth oscillation in the trace (379→432→391→408→435→400→434 KB at 6-second intervals) confirms NSCache is honoring `totalCostLimit` and evicting under pressure rather than allocating without bound. No leak signature. First sampler window (also 90 s) read flat 128 MB because the user hadn't started interacting yet — re-ran after confirming the app was at 377 MB to actually catch the peak. Wave 4.2 itself was the ~25 lines the plan called for, no surprises: (1) `ExtractionViewModel.captureFrame(at: CMTime)` mirrors `captureCurrentFrame()` but takes an arbitrary time, dedupes via `manualTimes.contains(time)`. (2) `ThumbnailCellView` gained a required `onCmdTap: (CMTime) -> Void` prop, a `@State isCommandHeld` tracked via `.onModifierKeysChanged(mask: .command, initial: false)` (the macOS-15-native API chosen during the Wave 4 plan rewrite), and a `NSEvent.modifierFlags.contains(.command)` synchronous read inside the tap closure as belt-and-suspenders fallback. (3) `ThumbnailStripView` gained a `vm: ExtractionViewModel` prop, an `isPinned(at:)` helper using a half-cell-interval window (`abs(CMTimeGetSeconds($0) - t) < 0.5 / model.density`) and wired both into the cell. (4) `LeftPaneView` passes `vm: vm`. SourceKit threw a wall of bogus "Cannot find type X" diagnostics during standalone-file edits — `xcodebuild` (which sees the full target) built clean both times. **Then the user asked for "a bare letter to also add manual" and proposed M.** Considered K (FCP mark-in convention) and B (bookmark) — both less mnemonic. Recommended bare `M` on the focused strip ScrollView (same pattern as the existing +/-/0 zoom keys, scoped to strip focus so it can't shadow text-field typing in the right pane). User said ship; added a fourth `.onKeyPress(keys: [.init("m"), .init("M")])` handler with the same `⌘/⌥/⌃` modifier-filter guards as +/-/0, routing to `vm.captureCurrentFrame()`. **Then the user surfaced the chronological-export mismatch from screenshot smoke:** right-pane manual list showed capture order (`00:13.853, 00:17.153, 00:18.471, 00:02.638, 00:00.000, …`) but `TimeListGenerator.times(for: .timestamps(...))` sorts ascending before feeding `images(for:)`, so filename indices mapped to chronological order — UI list order ≠ on-disk order. The aspirational "filename index preserves *requested* order" decision was in fact overridden by the upstream sort in `TimeListGenerator.swift` lines 32–43. Picked option A: factored `captureCurrentFrame()` and `captureFrame(at:)` through a new private `insertManualTime(_:)` helper that does sorted insert via `firstIndex { CMTimeGetSeconds($0) > CMTimeGetSeconds(time) }` — O(n) per insert, fine for typical N. Right-pane list is now a destination preview by construction. Side effect: `captureCurrentFrame()` now also dedupes (it didn't before; `captureFrame(at:)` already did). Trade-off accepted: the "remove the last row to undo the last capture" affordance is gone — strip cell-dot + footer-count already signal capture, so this is fine. Updated the misleading "Times sorted ascending … filename index preserves requested order" line in Key Decisions to reflect the new behavior.

2026-05-27 (evening) — **Phase 5 Wave 4.1 click-to-seek SHIPPED + active-index float bug fixed, all user-confirmed.** Plan-first approach: ran parallel `sosumi` + `apple-doc-mcp` + WebSearch streams before any code, then rewrote Wave 4 section of `PHASE5_PLAN_thumbnail_strip.md` to (a) fix path drift `UI/`→`Thumbnail/`, (b) drop the now-obsolete playhead-bar success criterion in favor of the active-cell border, (c) add `.allowsHitTesting(false)` on the decorative overlay as a first-class plan item (SwiftUI hit-tests by *layout shape, not visual shape* — confirmed via HWS + DevForums), (d) redirect Wave 4.2 modifier-key detection from the still-flaky-in-2025 `TapGesture.modifiers(.command)` to macOS-15-native `onModifierKeysChanged(mask: .command, initial: false)` with `NSEvent.modifierFlags` as fallback. Then implemented Wave 4.1 in ~15 lines: required `onTap: (CMTime) -> Void` prop on `ThumbnailCellView` + `.contentShape(Rectangle()).onTapGesture { onTap(time) }`; strip passes `player?.seek(to: tappedTime, toleranceBefore: .zero, toleranceAfter: .zero)` per Apple's *"Controlling the transport behavior of a player"* sample-accurate pattern; `.allowsHitTesting(false)` on the active-cell border overlay. **User smoke surfaced a one-cell-off bug** — traced not to geometry but to `activeIndex` math: at zoom 1, `density ≈ 1.516`, so cell timestamps don't align to 30 fps frame boundaries; sample-accurate seek lands on the frame whose *start* time is ε below the target; `floor(0.633 × 1.516) = 0` for what should have been cell 1. One-line fix: `.rounded(.down)` → `.rounded()`. User: "works now." Plan-first paid off — the `.allowsHitTesting(false)` and the modifier-detection redirect would have each been a second user-reported bug.

2026-05-27 (late afternoon) — **Phase 5 Wave 3 + mouse/keyboard zoom matrix + no-flash transitions SHIPPED, all user-confirmed.** Three connected pieces of work in one session: (1) Wave 3 shipped — `MagnifyGesture(minimumScaleDelta: 0.02)` on `LeftPaneView`'s outer VStack via `.simultaneousGesture`, paired with a baseline-snapshot fix in `StripModel.onZoomChange` (`magnification` is absolute from gesture start, so `zoomLevel * magnification` per tick compounded wildly — now `zoomBaseline * magnification` with snapshots in `onMagnifyStart` and `commitZoom`). (2) Mouse/keyboard zoom matrix added because pinch is trackpad-only — user picked all four input paths via multi-select: `View` menu ⌘=/⌘-/⌘0 via new `App/StripZoomActions.swift` (`@FocusedValue(\.stripZoom)` + `ZoomCommands: Commands`); bare `+`/`=`/`-`/`0` on the focused strip ScrollView via `.focusable() + .focusEffectDisabled() + .onKeyPress(keys: …)` with modifier-filter guards; ⌘+scroll-wheel via `NSEvent.addLocalMonitorForEvents` in a `.task` on the strip (pass-through by default, consume + dispatch to MainActor when ⌘ held); visible slider + reset (⟲) button in a new `zoomControlRow` under the grid. All four paths funnel through `StripModel.setZoom(_:)` which also re-syncs `zoomBaseline` so pinch-after-slider doesn't snap back. (3) Zoom-transition flash fix — user flagged "redrawing takes a bit of time"; root cause was that both `widthBucket` and density-driven `timeMillis` change at most transitions, so the new cache key misses entirely → placeholder flash. Fix: `ThumbnailCellView.bestImage()` cascades exact → same-time other-buckets `[240, 120, 60]` (SwiftUI scales) → `@State lastImage` (kept across cell rebinds via `.onChange(of: cellIdentity) { syncLastImage() }`). Cell-scoped state, ~5 MB extra resident for 100 visible cells, evaporates on LazyVGrid unload. User: "much better, no flashing now." All work was direct edits, no subagents (focused changes on files I'd already read).

2026-05-27 — **Phase 5 Wave 3 code-complete.** Direct edits (no subagents — two focused, well-scoped file changes). Wave 1.3 had already shipped most of the Wave 3.2 spec (density `pow(2, zoomLevel * 0.6)` capped at FPS, thumbWidth smoothstep, prefetch matrix ±50% / 0 / ±20%, 80ms debounce + cancel-replace, tolerance `±0.5s` density<5 else `.zero`). But `onZoomChange` had a latent bug: `MagnifyGesture.value.magnification` is **absolute** from gesture start (≈1.0 at start), so `zoomLevel * magnification` compounds wildly per tick. Fix: added `zoomBaseline`, captured in `onMagnifyStart` + `commitZoom`, applied as `zoomBaseline * magnification` in `onZoomChange`. Wave 3.1: `pinch` gesture as a computed property on `LeftPaneView` — `.updating($liveScale)`, `.onChanged` lazily fires `onMagnifyStart` once then calls `onZoomChange` per tick, `.onEnded` calls `commitZoom` then `onMagnifyEnd`. Attached via `.simultaneousGesture` to the loadedView VStack (NOT the ScrollView's children, per HWS forum #28798 + Apple Forums #760035 research). Build clean. Visual smoke deferred — pinch is interactive, can't be CLI-verified.

2026-05-27 — **Phase 5 Wave 2 shipped + pivoted to contact-sheet grid.** First cut (3 parallel subagents): horizontal filmstrip per the original plan — `ScrollView(.horizontal)` + `LazyHStack` cells + 2pt vertical playhead bar. Built clean, visual smoke confirmed thumbs right-side-up + correct aspect. **Pivot 1** (user: "smaller player, much more space for thumbnails"): wrapped player+strip in `VSplitView` (`autosaveSplitView(named: "LeftPaneVSplit")`), strip removed its hardcoded 120pt height and now flexes via the split divider (player `minHeight: 120`, strip `minHeight: 100, idealHeight: 320`). **Pivot 2** (user: "thumbnails should be several rows"): rewrote `ThumbnailStripView` as `ScrollView(.vertical) + LazyVGrid(columns: [GridItem(.adaptive(minimum: cellW, maximum: cellW), spacing: 1)])`; replaced the vertical playhead bar with an **active-cell border** (`strokeBorder(Theme.accent, lineWidth: 2)` around the cell at index `floor(currentTime × density)`); updated `onScrollGeometryChange` math to derive visible time range from `firstRow × cellsPerRow → lastRow × cellsPerRow`. **Width-collapse bug + fix:** `ScrollView(.vertical)` containing `LazyVGrid` reported a narrow intrinsic content width (= one cell), and `VSplitView` matched both panes to it → entire left pane collapsed to ~280pt wide. Fixed with `.frame(maxWidth: .infinity, maxHeight: .infinity)` on the ScrollView and `.frame(maxWidth: .infinity, alignment: .leading)` on the inner grid. User confirmed final layout works.

2026-05-27 (earlier same day) — **Phase 5 Wave 2 first cut shipped** (now superseded by the contact-sheet rewrite above). Three parallel subagents wrote `Thumbnail/ThumbnailCellView.swift` (44L, member-wise init, reads from cache, placeholder on miss, 4pt accent pin overlay in `.topTrailing` when `manualPinned`), `Thumbnail/ThumbnailStripView.swift` (first cut: `ScrollView(.horizontal)` + `LazyHStack` + vertical playhead bar; later rewritten), and modified `UI/LeftPaneView.swift` (+`AVFoundation` import, `@State stripModel: StripModel?`, `.task(id: vm.metadata) { rebuildStripModel() }`). `xcodegen generate` picked up the new files (sources glob auto-includes `Thumbnail/`).

2026-05-27 — **Phase 5 Wave 1 shipped.** Three parallel subagents wrote `Thumbnail/ThumbnailRenderer.swift`, `Thumbnail/ThumbnailCache.swift`, `Thumbnail/StripModel.swift` from the PHASE5 plan spec; folder consolidated to single-feature `Thumbnail/` (deviated from plan's `Core/App/UI` scatter). `xcodegen generate` picked them up automatically (sources glob already covered). `xcodebuild clean build` succeeded with zero warnings under `SWIFT_STRICT_CONCURRENCY=complete`.

2026-05-27 (late) — **Phase 4.5 polish CLOSED.** Six items shipped, build clean across every iteration, user confirmed all behaviors live.

- **Item 0 — unload-video gap:** `vm.clear()` + ✕ button. Root cause: AVPlayerView eats drag events before SwiftUI's `.onDrop` sees them.
- **Item 1 — `@AppStorage` persistence:** new `App/Preferences.swift` enum (UserDefaults helper). View-model properties declared without defaults, populated in `init()` from `Preferences.*()`, write-throughs via `didSet`. `OverlayPosition` made `String, CaseIterable` to round-trip. `manualTimes` intentionally left transient.
- **Item 2 — `project.yml` cleanup + rename:** dropped iOS-only `UIDesignRequiresCompatibility` cruft, set real copyright, fixed `CFBundleDisplayName` AND `CFBundleName` (menu bar reads the latter). Mid-pass the user noted "Screenshot" doesn't fit non-screen-recording video sources — **app renamed user-facing to "Stills From Video"** (internal IDs kept).
- **Item 3 — keyboard shortcuts:** lifted `vm` from ContentView to App, added `Commands { CommandGroup(replacing: .newItem) { Open Video… (⌘O) } }`. Esc cancels via `.keyboardShortcut(.cancelAction)`. Dropped ⌘⇧O for output folder as redundant per user feedback (right-pane button is right there; set-once).
- **Item 4 — app icon:** new `02_Design/generate_app_icon.swift` (~140 lines) renders 1024² master via `NSImage.lockFocus()` + `CGContext` (gradient bg, filmstrip with sprocket holes via even-odd fill, three dark frame windows, **detached warm-amber frame above-right with rotation + drop shadow** = the extraction gesture), then sips-resizes to 9 other slots. `02_Design/CLAUDE_DESIGN_PROMPT.md` staged for the polished replacement.
- **Item 5 — About panel:** covered by item 2's bundle-string fixes (default SwiftUI panel reads them).

Earlier the same day (late) — **Phase 4 closed out.** Wave 5.1 clean build, Wave 5.2 manual matrix a–g ALL PASSED, Wave 5.3 plan archived.

Earlier the same day (night) — Wrote full Phase 5 implementation plan (`/PHASE5_PLAN_thumbnail_strip.md`, 6 waves, 5 new files + 2 modified) via multi-stream research (Explore + general-purpose + apple-doc-mcp).

Earlier the same day (late evening) — Built Phase 4 Waves 2–4 (PlayerView, ExtractionViewModel, LeftPaneView, RightPaneView, SplitViewAutosave, ContentView HSplitView shell).

## Blockers
None.

## Next Actions

**Phase 5** ✅ CLOSED 2026-05-28. Waves 1–6 all shipped; manual test matrix passed; plan archived; decisions promoted to `docs/decisions.md`.

**Polish items 1, 2, 3** ✅ ALL SHIPPED 2026-05-28 on branch `polish/post-phase-5` (3 atomic commits, ~180 LOC net, build green, user-smoked live). See `sessions/2026-05-28.md` for detail.

**WebP export support — Waves A–D ALL DONE 2026-05-29 on `polish/webp-support`. Feature complete + smoke-verified. Remaining = housekeeping.** `POLISH_PLAN_webp_support.md` at repo root remains the authoritative spec.

- **Wave A** ✅ `5113627` — SPM wiring + stub encoder.
- **Wave B** ✅ `7fe7783` — encoder + model plumbing + the `"wrote N PNG"` message fix.
- **Wave C** ✅ `a9e53fe` (UI) + `207b2a6` (effort-split fix) — Lossless toggle, Quality↔Effort label flip, separate `exportEffort` value.
- **Wave D — smoke** ✅ PASSED 2026-05-29 on user's 4K Rec.709 clip (`cf7a98b` tooltip fix). Lossy good; formats valid; effort knob works; no color cast. **Lossless is near-lossless (≤5/255), not bit-exact** (shift in the CGImage→NSImage→SDWebImage encode hand-off, confirmed via libwebp's `dwebp`) — accepted as-is per user, tooltip corrected. See Last Session.
- **Wave E:** subsumed by the per-wave commit pattern; no separate "ship" wave needed unless squashing is wanted.
- **Decisions to promote to `docs/decisions.md` post-ship** (now includes 3 execution findings): the 5 in the plan's "Decisions to promote" section, PLUS (a) the **0.15.0 option-constant rename** (`.encodeWebPMethod` not `SDImageCoderEncodeWebPMethod`), (b) the **`exportEffort`-separate-from-quality** decision (default 0.75; one slider, two backing values), and (c) **WebP "lossless" is near-lossless ≤5/255** in this pipeline (NSImage hand-off), not bit-exact — tooltip says "visually identical".
- **Merge/push decision** for `polish/webp-support` (6 code + 4 doc commits; local-only, no remote).

**Immediate follow-ups (housekeeping — not blocking WebP):**
1. **Promote remaining 2026-05-28 polish decisions to `docs/decisions.md`** (xcodegen-regen-required ✅ already promoted 2026-05-28 and reconfirmed during Wave A; WebP omission ✅ already promoted but will need a "reopened by polish/webp-support" addendum once WebP ships):
   - JPEG `.jpg` (not `.jpeg`) extension override; single shared quality across JPG/HEIC; PNG default; quality slider hidden (not disabled) when PNG selected; "Unload" label choice; format-section placement between Output and Options.
2. **Archive `POLISH_PLAN_post_phase5.md`** → `docs/sessions/_archive/2026-05-28_POLISH_PLAN_post_phase5.md` (matches Phase 4/5 plan-archival convention).
3. **Decide fate of 6 smoke-test export files in repo root** (`ExportHEIC0/50.heic`, `exportHEIC100.heic`, `ExportJPEG0/50/100.jpg`): delete, move under `04_Exports/`, or add patterns to `.gitignore`.
4. **Merge or push** `polish/post-phase-5` (and eventually `polish/webp-support`). No remote currently configured; user-judgment call whether to set one up or keep this single-developer with merges to main.

**Carry-over tuning ideas** (low-priority, not blocking anything):
1. Consider raising default `zoomLevel` from 1.0 to ~3 (cells go 60→~95pt) for a friendlier first view.
2. Re-tune prefetch pads (±50% / 0 / ±20%) — calibrated for a horizontal-row layout, may want wider pads for the contact-sheet grid.
3. Tune the 256 in-flight decode cap if either too aggressive (visible lag at edges) or too lax (CPU spikes return on different hardware).

**Next polish queue (none blocking):**
- Sparkle auto-update (only matters once distributing real builds).
- Diagnostic logging (optional for a personal tool; relevant if app is shared).
- Notarized DMG (final distribution step).
- Light-mode toggle (see Future ideas).
- Polished app icon — drop the Claude Design output into `icon_512x512@2x.png` and run the sips loop from `02_Design/CLAUDE_DESIGN_PROMPT.md`.

## Key Decisions
- Build tooling: **xcodegen** (`01_Project/project.yml`) — regenerable and reviewable
- macOS 15.0+ / Swift 6.0 / SwiftUI / notarized non-sandboxed
- Bundle ID: `com.lucesumbrarum.ScreenshotFromVideos`, team `FDMSRXXN73`
- **Lift strategy:** distill, don't copy whole. Each lifted file ≤120 lines, single-purpose, with a header comment naming its source
- **Extraction mode:** segmented picker — Interval (seconds / frames) OR Manual timestamps
- **Batch frame API:** `AVAssetImageGenerator.images(for: [CMTime])` (macOS 13+ AsyncSequence). The older callback API crashes under Swift 6 strict concurrency (Apple DTS #761194)
- **Concurrency shape:** `@MainActor @Observable` view-model owning one `Task<Void, Never>`; per-iter `@MainActor` progress callback; no custom actor, no AsyncStream
- **Cancellation:** `withTaskCancellationHandler { cancelAllCGImageGeneration() }` + `Task.checkCancellation()`. Partial PNGs preserved on cancel
- **TaskGroup parallelism rejected** for frame extraction — `images(for:)` exploits sequential GOP decoding internally
- **Times sorted ascending** before passing to `images(for:)` (in `TimeListGenerator.times(for: .timestamps(_:))`); filename indices are then assigned in delivery order, so on-disk numbering is chronological by clip time. The right-pane manual list also sorts on insert (since 2026-05-28) so the list row order matches what export writes
- **Filename collisions:** `_1`, `_2`, … suffix pattern; never overwrite
- **`autoreleasepool` only around the PNG encode step**; Swift Concurrency handles the rest on Darwin
- **Default tolerance `.zero`** but configurable per request
- **(Wave 2)** Discrete UI fields (`tab/intervalUnit/intervalSeconds/intervalFrames/manualTimes`) are the source of truth; `buildRequest()` resolves them into the locked `ExtractionMode` at Export time. No redundant `mode` field on the view-model
- **(Wave 3)** Live current-time label uses `TimelineView(.periodic)` over `addPeriodicTimeObserver` — no observer token to manage, no leak risk
- **(Wave 4)** Skipped `.environment(\.theme, .dark)` from the plan — `Theme` is a static enum, not an `EnvironmentKey`. Views read `Theme.primaryBackground` directly. Revisit if/when user-customizable accents are needed
- **(Wave 4)** `SplitViewAutosaveHelper` lifted verbatim from cookbook `01-window-layouts.md`. Divider position persists via `NSSplitView.autosaveName = "MainSplit"`
- **(Phase 4.5)** **App renamed user-facing to "Stills From Video"**. Internal identifiers (bundle ID `com.lucesumbrarum.ScreenshotFromVideos`, repo folder, Xcode target, scheme) kept. Only `CFBundleName` and `CFBundleDisplayName` changed. Rationale: "Screenshot" implies screen capture but the app accepts arbitrary video files; "Still" is the established cinematography term for an extracted frame
- **(Phase 4.5)** **Preferences pattern:** dedicated `App/Preferences.swift` enum with `UserDefaults` read/write static funcs. View-model properties declared without default values, populated in `init()` from `Preferences.*()`, write-throughs via `didSet`. The no-default trick avoids spurious writes during init (Swift skips `didSet` on first assignment in init). `nonisolated(unsafe) static let defaults = UserDefaults.standard` is the Swift-6 strict-concurrency escape hatch — `UserDefaults` is documented thread-safe but Foundation doesn't conform it to `Sendable`
- **(Phase 4.5)** **`@State var vm` lives at App level**, not inside ContentView. Required for the `Commands { ... }` block on `WindowGroup` to capture the same view-model instance. Single-window app — multi-window would need `@FocusedValue` instead
- **(Phase 4.5)** **Menu bar uses `CFBundleName`, not `CFBundleDisplayName`.** To rename the user-facing app cleanly, set both bundle strings to the same value. `PRODUCT_NAME` / bundle ID / target name don't need to match — those are internal
- **(Phase 4.5)** **No ⌘⇧O for output folder.** The right-pane Choose folder button is right there and users set the output folder once per session — a menu duplicate would be redundant
- **(Phase 4.5)** **Icon strategy: placeholder generator + Claude Design prompt** in `02_Design/`. The placeholder is reproducible (`swift 02_Design/generate_app_icon.swift`); the polished version is one prompt away. Placeholder visual: dark navy gradient + filmstrip (sprocket holes punched via even-odd fill) + a detached warm-amber frame above-right (the extraction gesture)
- **(Phase 5 Wave 2)** **Contact-sheet grid over horizontal filmstrip.** Original plan called for a single-row filmstrip (iMovie/FCP precedent). User asked for multi-row wrapping after seeing the empty vertical space; `LazyVGrid(.adaptive)` with active-cell border replaces `LazyHStack` + vertical playhead. The grid pattern matches QuickTime's keyframe-export UI and the Photos browser — also valid prior art
- **(Phase 5 Wave 2)** **Player ↔ strip split is user-adjustable.** Wrapped both in a `VSplitView` with `autosaveSplitView(named: "LeftPaneVSplit")`. Player `minHeight: 120`, strip `minHeight: 100, idealHeight: 320`. Footer stays outside the split, below both. Same `SplitViewAutosaveHelper` lifted from cookbook 01-window-layouts.md works for vertical splits too
- **(Phase 5 Wave 2)** **`.frame(maxWidth: .infinity)` on vertical scroll containers inside `VSplitView`.** `ScrollView(.vertical)` containing a `LazyVGrid` reports a narrow intrinsic width (= one cell), and `VSplitView` matches both panes to it. Without an explicit horizontal-fill modifier on the scroll container, the entire split column collapses to that narrow width. Worth remembering for any future split-view + vertical-scroll combos
- **(Phase 5 Wave 3)** **MagnifyGesture: snapshot the baseline, don't compound.** `MagnifyGesture.value.magnification` is the **absolute** scale from gesture start (1.0 at start, grows or shrinks). Apply it against a snapshot of `zoomLevel` taken in `onMagnifyStart`, NOT against the current `zoomLevel` (which would compound each tick and blow past the clamp). `StripModel.zoomBaseline` is the snapshot. Symptom of the wrong pattern: even tiny pinches send zoom straight to the max clamp within ~50ms. `commitZoom` also re-snaps the baseline so the next gesture starts clean
- **(Phase 5 Wave 3+)** **All zoom input paths converge on `setZoom(_:)`** — menu commands, keyboard, scroll wheel, and slider all route through this one mutator (not just `onZoomChange`, which is pinch-only). `setZoom` clamps to [1, 12] AND updates `zoomBaseline` to the new value, so a subsequent pinch starts from the user's last set point. Without that baseline sync, pinching after a slider drag would snap back to whatever was the baseline at last `onMagnifyStart`
- **(Phase 5 Wave 3+)** **`@FocusedValue` for view-scoped menu commands.** The `View > Zoom` menu items can't bind directly to `StripModel` because the model lives inside `LeftPaneView` (`@State private var stripModel: StripModel?`), not at App level. Hoisting it to App level would require also hoisting the metadata-driven lifecycle. Instead: `LeftPaneView` publishes a `StripZoomActions` struct via `.focusedSceneValue(\.stripZoom, …)`; `ZoomCommands` reads via `@FocusedValue(\.stripZoom)` and `.disabled(zoom == nil)` greys the menu out when no video is loaded. Cleaner than hoist + closure-prop
- **(Phase 5 Wave 3+)** **`.onKeyPress` overload disambiguation.** Single-key form `.onKeyPress(_:)` defaults to a zero-arg action closure; to receive a `KeyPress` argument (for modifier filtering) either pass `phases: .down` explicitly OR use `.onKeyPress(keys: [...])` — the latter only has the `(KeyPress) -> Result` overload, so it disambiguates cleanly. Chose `keys:` form throughout for consistency
- **(Phase 5 Wave 3+)** **⌘+scroll-wheel via NSEvent local monitor, not NSViewRepresentable.** Tried mentally: NSViewRepresentable overlay needs `nextResponder?.scrollWheel(with:)` pass-through, which is fragile in SwiftUI's responder chain. Cleaner pattern is `NSEvent.addLocalMonitorForEvents(matching: .scrollWheel)` in a `.task` on the strip view — returns the event by default (pass-through), returns `nil` to consume (when ⌘ held). Scoped to the strip's lifetime: the task is cancelled when the strip leaves the view tree, the monitor is removed in `defer`. Inside the closure, `Task { @MainActor in m.applyZoomDelta(dy) }` hops to MainActor for the model call
- **(Phase 5 Wave 3+)** **Cell-side fallback cascade kills the zoom-transition flash.** Cells now resolve via `bestImage()`: exact `(time, bucket)` → same time at other buckets `[240, 120, 60]` (SwiftUI's `scaledToFill` handles the resize — scaling down is visually free) → `@State lastImage: CGImage?` (the last successfully-displayed exact-match, kept across `(time, widthBucket)` rebinds via `.onChange(of: cellIdentity) { syncLastImage() }`). Alternatives rejected: snap timestamps to a 100ms base grid (only helps zoom-out, not zoom-in); centralized nearest-time lookup (NSCache doesn't support iteration → would need a parallel index). `lastImage` is cell-scoped and evaporates on LazyVGrid lazy unload, so no memory leak — ~5 MB extra resident for 100 visible cells
- **(Phase 5 Wave 4.1)** **`.rounded()`, not `.rounded(.down)`, in `activeIndex`.** AVPlayer's sample-accurate seek with `.zero/.zero` tolerance lands on the frame *containing* the target time, and reports `currentTime()` as that frame's *start* time — which is ε below the target whenever the target doesn't land exactly on a frame boundary (the common case at fractional `density`, e.g. `pow(2, 0.6) ≈ 1.516`). With `.rounded(.down)`, that ε pushed `activeIndex` one cell to the left of the clicked cell. Round-to-nearest tolerates the undershoot and matches the user's mental model ("the cell whose timestamp is closest to currentTime"). Trade-off during natural playback: the border now flips at the midpoint of each cell's time range instead of at its end — subjectively feels more responsive, not less correct
- **(Phase 5 Wave 4.1)** **`.allowsHitTesting(false)` on decorative overlays inside tap-handling views, as a rule.** SwiftUI hit-tests by **layout shape, not visual shape** (HWS + Apple DevForums). A `Rectangle().strokeBorder(...)` overlay has a full-rect hit region despite painting only a thin border line — without the modifier, taps on the active cell would have been swallowed by its own decorative border. Applies to any overlay that's purely visual
- **(Phase 5 Wave 4.1)** **Sample-accurate seek (`.zero/.zero`) is the right contract for click-to-seek, even with the activeIndex headache.** Considered switching to `model.tolerance` (which is `±0.5 s` at low zoom) to match the strip renderer's own tolerance, but the user clicked a *specific* thumb — they expect to land on its frame, not the nearest keyframe minus 0.5 s. Fix the visual-feedback math, not the seek precision. Apple's *"Controlling the transport behavior of a player"* article confirms this is the canonical pattern; the decoding-delay cost cited there only matters for continuous-scrub UIs
- **(Phase 5 Wave 4.1)** **Modifier-key detection in SwiftUI: prefer `onModifierKeysChanged(mask:initial:_:)` (macOS 15+) over `TapGesture.modifiers(_:)`.** The latter has been flaky on macOS for years (Apple DevForums #654154, ongoing 2024–2025). `onModifierKeysChanged` tracks the held state into `@State`, branched inside `.onTapGesture`. `NSEvent.modifierFlags.contains(.command)` read synchronously at tap time is the second-tier fallback
- **(Phase 5 Wave 4.2)** **Wave 4.2 used both `onModifierKeysChanged` AND a synchronous `NSEvent.modifierFlags` read inside the tap closure, belt-and-suspenders.** The state-tracked variable handles the normal case; the in-closure read covers focus-race cases where the strip ScrollView regains focus mid-tap and the change handler hasn't yet fired with the current modifier set. Both paths route to `onCmdTap` — no double-fire risk because the tap gesture fires once
- **(Phase 5 Wave 4.2)** **Manual-pin half-interval window in seconds, not CMTime.** Cell pin renders when `abs(CMTimeGetSeconds($0) - cellTimeSec) < 0.5 / model.density`. CMTime arithmetic via `CMTimeSubtract` + `CMTimeAbsoluteValue` would work but Double seconds is cleaner since we already work in seconds in `activeIndex`, `thumbnailTimes`, etc. Resolved at the strip level — cell stays vm-unaware (takes a `manualPinned: Bool`, not the model)
- **(Phase 5 — extra capture shortcut)** **Bare `M` on the focused strip routes to `vm.captureCurrentFrame()`.** Same `.onKeyPress(keys:)` pattern as the +/-/0 zoom keys, with the same `⌘/⌥/⌃` modifier-filter guards so it doesn't shadow accelerator combos elsewhere. Scoped to strip focus rather than app-wide (via Commands menu / window-level `.keyboardShortcut(modifiers: [])`) on purpose: a bare-letter app-wide shortcut would swallow `M` typed into the right-pane interval TextFields. The strip is where the user naturally has focus while scrubbing
- **(Phase 5 Wave 5)** **Placeholder must be visually distinct from the strip's own background.** Both used `Theme.secondaryBackground` (0.15 grey) → empty cells were invisible against the strip. Now placeholder is `Color(white: 0.07)`; the 1 pt cell-spacing gap shows the lighter strip background through, so the empty grid reads as "many dark cells separated by lighter dividers" → "loading", not "void". When the strip is genuinely empty (no video loaded), the strip's `Theme.secondaryBackground` fill is what shows — no placeholder cells exist
- **(Phase 5 Wave 5)** **Debounce only while actively interacting.** `StripModel.scheduleRender()` now gates its 80 ms `Task.sleep` on `isScrolling || isMagnifying`. Debounce kicks in during continuous scroll/pinch (good — prevents render thrash that was preventing *any* rendering during scrub). The moment either phase ends, the next `scheduleRender` call (triggered by `onScrollPhaseChange(false)` or `onMagnifyEnd`) reads both flags false → skips the sleep → renders immediately. End-to-end latency from scroll-release to first thumbs ≤1 frame instead of 80 ms + decode lag
- **(Phase 5 Wave 5)** **Player-seek → strip auto-scroll via delta-detection on the periodic time observer.** Natural per-tick deltas at the 1/30 s observer interval are ≤33 ms; anything >0.5 s is treated as a seek (user clicked the AVPlayer seek bar, or click-to-seek on an off-screen thumb). Setting `@State seekScrollTarget = activeIndex` then triggers a `ScrollViewReader`-backed `.onChange` that animates `proxy.scrollTo(target, anchor: .center)` over 0.15 s. Doesn't fire during natural playback (so user's manual strip scroll position is respected during playback), but does fire on every seek including the strip's own click-to-seek (gives a small "recenter after click" affordance, kept on purpose)
- **(Phase 5 Wave 5)** **Design target is 30 s–2 min clips; long-clip optimization out of scope.** Memory smoke held at peak 466 MB on a 64 GB box (ceiling 700 MB), but qualitative render lag at 40-min stress was real. User confirmed the app's target use case is short clips — the multi-hour-stress scenario is acknowledged-imperfect and explicitly not on the optimization roadmap. Mitigations would be (i) two-task split: hot visible-only renderer + cold prefetch, (ii) coarser default density at high duration, (iii) cap visible cells via virtualized rows. None of these are planned

- **(Post-Phase 5 polish plan — REOPENED 2026-05-29 via `POLISH_PLAN_webp_support.md`)** **WebP dropped from SFV's planned format list.** The conclusion has been reversed: WebP is being added via SDWebImageWebPCoder 0.15.0 (SPM dep, ~2 MB binary growth) — the user's paid Apple Developer Program account makes notarization + hardened runtime non-issues. The underlying ImageIO-can't-write-WebP fact remains correct and is exactly why a third-party encoder is needed. ImageIO has no WebP write support on macOS through 26.5 — runtime-probed via `swift /tmp/probe.swift`: `CGImageDestinationCopyTypeIdentifiers()` returns 22 writable types and `org.webmproject.webp` is not among them (HEIC/JPEG/PNG/TIFF are). `UTType.webP` exists since macOS 11 but is decode-only. Apple DevForum #688001 documents this back to 2021; no encoder mentioned in macOS 14/15/26 release notes. CropBatch's `Models/ExportSettings.swift` lists `.webp` and `ImageCropService.encode/save()` calls `CGImageDestinationCreateWithURL(..., UTType.webP.identifier, …)` directly with no vendored encoder (Package.resolved has only Sparkle) and no fallback — the destination create returns nil, the error is logged at `CropBatchApp.swift:368` and silently swallowed. CropBatch's README "WebP" claim is hollow; no `.webp` file is ever written. SFV's plan ships PNG/JPG/HEIC; vendoring [SDWebImageWebPCoder](https://github.com/SDWebImage/SDWebImageWebPCoder) (~200 KB libwebp) documented as a deferred option if WebP becomes non-negotiable later — adds notarization/hardened-runtime burden, defensible only if asked. See `POLISH_PLAN_post_phase5.md` Item 2.

- **(Post-Phase 5 polish plan)** **JPEG file extension hardcoded to `.jpg`, overriding `UTType.preferredFilenameExtension`.** `UTType.jpeg.preferredFilenameExtension` returns `"jpeg"`, but convention (and macOS Finder norms) is `.jpg`. `ExportFormat.fileExtension` in the plan maps `.jpeg → "jpg"` explicitly. Other formats use the preferred extension as-is (`.png → "png"`, `.heic → "heic"`).

- **(Phase 5 — sorted manual list)** **Manual list is sorted by clip time on insert, not in capture order.** Both `captureCurrentFrame()` and `captureFrame(at:)` route through a private `insertManualTime(_:)` helper that finds the sorted position via `firstIndex { CMTimeGetSeconds($0) > CMTimeGetSeconds(time) }` and `insert(_:at:)`. Reason: the right-pane "Manual Timestamps" list is a destination preview — row N corresponds to filename `_N`. The aspirational "filename index preserves *requested* order" decision was overridden in practice by the existing ascending sort in `TimeListGenerator.times(for: .timestamps(_:))`, so aligning the UI to that is simpler than removing the upstream sort (which `images(for:)` benefits from for sequential GOP decoding anyway). Side effect of the helper: `captureCurrentFrame()` now also dedupes (it didn't before)

- **(Post-Phase 5 polish — xcodegen)** **xcodegen regen IS required for new files in this repo.** The sources glob in `project.yml` is resolved at `xcodegen generate` time, not at `xcodebuild` time. Adding a new `.swift` file under `01_Project/ScreenshotFromVideos/` does NOT auto-include it in the build — `xcodebuild` will fail with "cannot find type X." Run `cd 01_Project && xcodegen generate` before the next build whenever a file is added (or moved). Verified empirically during Item 2 implementation (the new `Models/ExportFormat.swift` triggered exactly this failure on first build). `POLISH_PLAN_post_phase5.md`'s "auto-discovers — no `xcodegen generate` needed" claim was wrong; this entry is the authoritative correction

- **(Post-Phase 5 polish — Item 2 format section placement)** **Format section sits between Output and Options in RightPaneView, not nested inside the Options DisclosureGroup.** Mode → Params → Output → Format → Options reads as "what / when / where / how" in left-to-right precedence. Format materially changes file size and on-disk extension, so it belongs at the top level alongside the destination folder — not nested under "Options" (which is about cosmetic processing like overlay and numbering)

- **(Post-Phase 5 polish — quality slider visibility)** **Quality slider is hidden, not disabled, when PNG is selected.** Implemented as a SwiftUI `if vm.exportFormat.supportsCompression { ... }` branch inside `formatSection`. Rationale: PNG is always lossless, the slider would have no meaning if shown grey'd-out. The layout shift is small (slider row is single-line). Matches CropBatch's UX pattern

## Future ideas (Phase 5+)

### Thumbnail strip with zoom-density coupling — **Phase 5 IN PROGRESS (Waves 1–3 + UX polish + Wave 4.1 + 4.2 shipped; Waves 5–6 next)**
Plan in `/PHASE5_PLAN_thumbnail_strip.md` (Wave 4 section rewritten 2026-05-27 evening with current Apple-doc + community findings — paths, success criteria, hit-testing fix, modifier-key strategy). Waves 1 (foundations), 2 (static grid), 3 (MagnifyGesture + zoom-density coupling), 4.1 (click-to-seek) and 4.2 (⌘-click capture) all shipped, plus an extra bare-`M` capture shortcut on the focused strip (not in original plan — added on user request 2026-05-28). Mouse/keyboard zoom input matrix (menu / bare keys / ⌘+scroll-wheel / slider) added on top of Wave 3 — not in original plan. Cell-side fallback cascade added to eliminate zoom-transition flash — also not in plan. **Deviations from original plan:** strip is a contact-sheet grid (`LazyVGrid`) not a single-row filmstrip; pinch is one of four zoom inputs not the sole one; Wave 4.2 modifier-key strategy switched to `onModifierKeysChanged` per current DevForums signal on `TapGesture.modifiers(.command)` flake — see Key Decisions. Decisions blocks in `sessions/2026-05-27.md` + `sessions/2026-05-28.md` still need promotion to `docs/decisions.md` (file still doesn't exist).

### Light-mode toggle — **deferred past current polish list**
Idea recorded 2026-05-27. App is currently hard-coded to `.preferredColorScheme(.dark)` on the root view in `ScreenshotFromVideosApp.swift`. User would like a toggle to switch to light mode (or "follow system"). Most natural placement: View menu or an Appearance section in an eventual Settings scene. Comes after the locked Phase 4.5 polish list above; not Phase 5 either.

**Implementation notes for later:**
- `@AppStorage("colorScheme")` storing `system` / `light` / `dark`.
- Apply via `.preferredColorScheme(...)` in the app shell (`nil` for system, `.light`/`.dark` for explicit).
- `Theme` is currently a static enum reading static color literals. If we want true per-scheme tokens (different `primaryBackground` etc. in light vs dark), `Theme` needs to become an `EnvironmentKey`-backed struct — consciously deferred during Wave 4 of Phase 4 for this exact case. Light mode is the right moment to revisit that.
