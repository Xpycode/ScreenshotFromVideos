# 📦 Package Notes — ScreenshotFromVideos

> Surfaced at session start. Part of the zPackages initiative.
> Catalog (source of truth): /Users/sim/ProgrammingProjects/1-macOS/zPackages/docs/PACKAGE-CATALOG.md
> Last survey: 2026-05-30.

## Adopt (consume these in this app)
- **MediaKit** — AV probe + thumbnail/frame generation — candidate
- **FolderAccessKit** — security-scoped bookmark / drag-drop folder access — candidate

## Source for (extract these FROM this app)
- **MediaKit** — `FrameExtractor` / `ThumbnailRenderer` / `ThumbnailCache` — canonical thumbnailer
- **ImageIOKit** — `ImageExportService` + `WebPEncoder` — multi-format image encode/save

## Notes
- —
